///
/// HybridTarBz2Spec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `TarBz2Result` to properly resolve imports.
namespace margelo::nitro::tarbz2 { struct TarBz2Result; }

#include "TarBz2Result.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include <vector>
#include <functional>

namespace margelo::nitro::tarbz2 {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `TarBz2`
   * Inherit this class to create instances of `HybridTarBz2Spec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridTarBz2: public HybridTarBz2Spec {
   * public:
   *   HybridTarBz2(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridTarBz2Spec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridTarBz2Spec(): HybridObject(TAG) { }

      // Destructor
      ~HybridTarBz2Spec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<TarBz2Result>> unpack(const std::string& sourcePath, const std::string& targetPath, bool overwrite) = 0;
      virtual std::shared_ptr<Promise<TarBz2Result>> pack(const std::string& sourcePath, const std::string& targetPath) = 0;
      virtual std::shared_ptr<Promise<std::vector<std::string>>> listContents(const std::string& sourcePath) = 0;
      virtual std::shared_ptr<Promise<TarBz2Result>> unpackWithProgress(const std::string& sourcePath, const std::string& targetPath, bool overwrite, const std::function<void(double /* progress */)>& onProgress) = 0;
      virtual std::shared_ptr<Promise<TarBz2Result>> packWithProgress(const std::string& sourcePath, const std::string& targetPath, const std::function<void(double /* progress */)>& onProgress) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "TarBz2";
  };

} // namespace margelo::nitro::tarbz2
