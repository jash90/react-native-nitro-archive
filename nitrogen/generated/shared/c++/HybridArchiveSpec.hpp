///
/// HybridArchiveSpec.hpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#pragma once

#if __has_include(<NitroModules/HybridObject.hpp>)
#include <NitroModules/HybridObject.hpp>
#else
#error NitroModules cannot be found! Are you sure you installed NitroModules properly?
#endif

// Forward declaration of `ArchiveResult` to properly resolve imports.
namespace margelo::nitro::archive { struct ArchiveResult; }
// Forward declaration of `ArchiveOptions` to properly resolve imports.
namespace margelo::nitro::archive { struct ArchiveOptions; }
// Forward declaration of `ArchiveFormat` to properly resolve imports.
namespace margelo::nitro::archive { enum class ArchiveFormat; }
// Forward declaration of `ArchiveEntry` to properly resolve imports.
namespace margelo::nitro::archive { struct ArchiveEntry; }

#include "ArchiveResult.hpp"
#include <NitroModules/Promise.hpp>
#include <string>
#include "ArchiveOptions.hpp"
#include <optional>
#include <functional>
#include "ArchiveFormat.hpp"
#include "ArchiveEntry.hpp"
#include <vector>

namespace margelo::nitro::archive {

  using namespace margelo::nitro;

  /**
   * An abstract base class for `Archive`
   * Inherit this class to create instances of `HybridArchiveSpec` in C++.
   * You must explicitly call `HybridObject`'s constructor yourself, because it is virtual.
   * @example
   * ```cpp
   * class HybridArchive: public HybridArchiveSpec {
   * public:
   *   HybridArchive(...): HybridObject(TAG) { ... }
   *   // ...
   * };
   * ```
   */
  class HybridArchiveSpec: public virtual HybridObject {
    public:
      // Constructor
      explicit HybridArchiveSpec(): HybridObject(TAG) { }

      // Destructor
      ~HybridArchiveSpec() override = default;

    public:
      // Properties
      

    public:
      // Methods
      virtual std::shared_ptr<Promise<ArchiveResult>> unpack(const std::string& sourcePath, const std::string& targetPath, bool overwrite, const std::optional<ArchiveOptions>& options, const std::optional<std::function<void(double /* bytesProcessed */, double /* totalBytes */)>>& onProgress) = 0;
      virtual std::shared_ptr<Promise<ArchiveResult>> pack(const std::string& sourcePath, const std::string& targetPath, ArchiveFormat format, const std::optional<ArchiveOptions>& options, const std::optional<std::function<void(double /* bytesProcessed */, double /* totalBytes */)>>& onProgress) = 0;
      virtual std::shared_ptr<Promise<std::vector<ArchiveEntry>>> listContents(const std::string& sourcePath, const std::optional<ArchiveOptions>& options) = 0;
      virtual std::shared_ptr<Promise<ArchiveFormat>> detectFormat(const std::string& sourcePath) = 0;
      virtual std::shared_ptr<Promise<bool>> testArchive(const std::string& sourcePath, const std::optional<std::string>& password) = 0;

    protected:
      // Hybrid Setup
      void loadHybridMethods() override;

    protected:
      // Tag for logging
      static constexpr auto TAG = "Archive";
  };

} // namespace margelo::nitro::archive
